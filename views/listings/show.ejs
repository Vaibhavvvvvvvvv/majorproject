<% layout("/layouts/boilerplates") %>
<body>
    <h2 class="mb-3 text-center">Listing Details</h2>
    <div class="container mt-4"> <!-- Bootstrap container for layout and spacing -->
      
        <div class="container mt-4"> <!-- Bootstrap container for layout and spacing -->
            <div class="card border-0 mx-auto" style="max-width: 400px; background-color: #f8f9fa;"> <!-- Center the card, limit card width, hide the border, and set background color -->
                <div class="card-body">
                     <!-- Centered heading within the card body -->   
                    <div class="row justify-content-center mb-3"> <!-- Center the image -->
                        <div class="col-md-8"> <!-- Use smaller column width -->
                            <img src="<%= listing.image %>" class="card-img-top" alt="Listing Image" style="max-width: 100%; height: auto;"> <!-- Adjusted height to maintain aspect ratio -->
                        </div>
                    </div>
    
                    <ul class="list-group"> <!-- Bootstrap list group for consistent styling -->
                        <li class="list-group-item">Title: <%= listing.title %></li> <!-- Add label for clarity -->
                        <li class="list-group-item">Description: <%= listing.description %></li> <!-- Use list-group-item for consistent styling -->
                        <li class="list-group-item">Price: ₹ <%= listing.price.toLocaleString("en-IN") %></li> <!-- Format with currency symbol -->
                        <li class="list-group-item">Location: <%= listing.location %></li>
                        <li class="list-group-item">Country: <%= listing.country %></li>
                    </ul>
                </div>
            </div>
        </div>
        <div class="row justify-content-center mt-4"> <!-- Center the edit and delete actions -->
            <div class="col-md-6 text-center"> <!-- Use medium-sized column for better layout -->
                <!-- Bootstrap button classes for consistent styling -->
                <a href="/listings/<%= listing._id %>/edit" class="btn btn-secondary">Edit This Listing</a>

                <!-- Form with method override to delete the listing -->
                <form method="POST" action="/listings/<%= listing._id %>?_method=DELETE" class="d-inline"> <!-- Use inline display to keep buttons on the same line -->
                    <button type="submit" class="btn btn-danger">Delete</button> <!-- Bootstrap danger class for delete button -->
                </form>
            </div>
        </div>
    </div>

    <!-- Include Bootstrap JS and dependencies -->
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
