<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wanderlust</title>
    <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/css/bootstrap.min.css">
</head>
<body>
    <h1>All Listings</h1>
    <div class="container mt-4"> <!-- Bootstrap container for layout -->
        <!-- Form to create a new listing -->
        <form method="GET" action="/listings/new"> <!-- Corrected method to GET -->
            <!-- Bootstrap button with primary style -->
            <button type="submit" class="btn btn-primary">Create New Listing</button>
        </form>
    </div>
    <ul>
        <% if (allList && allList.length > 0) { %>
            <% for (let listing of allList) { %>
                <li>
                    <a href="/listings/<%= listing._id %>"><%= listing.title %></a>
                  </li>
                  
            <% } %>
        <% } else { %>
            <li>No listings available</li>
        <% } %>
    </ul>
    <script src="https://code.jquery.com/jquery-3.5.1.slim.min.js"></script>
    <script src="https://stackpath.bootstrapcdn.com/bootstrap/4.5.2/js/bootstrap.min.js"></script>
</body>
</html>
